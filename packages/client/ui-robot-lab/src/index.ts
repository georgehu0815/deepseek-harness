/** Host entry; all Robot Lab presentation is loaded by the client plugin graph. */
export const name = 'client-ui-robot-lab'
/** No host services are consumed by the presentation entry. */
export const inject: string[] = []
/** Mount the browser-only package's empty host entry. */
export function apply(): void {}
